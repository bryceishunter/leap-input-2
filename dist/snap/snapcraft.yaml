name: leapdesk-kvm
base: core18
version: master
version-script: git describe --tags | sed "s/^v//"
adopt-info: leapdesk-kvm
grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots
icon: res/io.github.bryceishunter.leapdesk-kvm.svg
license: GPL-2.0

apps:
  leapdesk-kvm:
    command: desktop-launch leapdesk #first run might take longer
    environment:
      # Fallback to XWayland if running in a Wayland session.
      DISABLE_WAYLAND: 1
    desktop: usr/share/applications/io.github.bryceishunter.leapdesk-kvm.desktop
    common-id: io.github.bryceishunter.leapdesk-kvm
    plugs: &plugs
      - desktop
      - desktop-legacy
      - home
      - joystick
      - network
      - opengl
      - pulseaudio
      - screen-inhibit-control
      - unity7
      - wayland
      - x11
  leapdesk-client:
    command: desktop-launch leapdesk-client
    plugs: *plugs
  leapdesk-server:
    command: desktop-launch leapdesk-server
    plugs: *plugs

parts:
  desktop-qt5:
    source: https://github.com/ubuntu/snapcraft-desktop-helpers.git
    source-subdir: qt
    plugin: make

  leapdesk-kvm:
    source: .
    source-type: git
    plugin: cmake
    parse-info: [usr/share/metainfo/io.github.bryceishunter.leapdesk-kvm.appdata.xml]
    configflags:
      - "-DQT_DEFAULT_MAJOR_VERSION=5"
      - "-DCMAKE_INSTALL_PREFIX=/usr"
      - "-DCMAKE_BUILD_TYPE=Release"
    build-packages:
      - build-essential
      - xorg-dev
      - libcurl4-openssl-dev
      - libavahi-compat-libdnssd-dev
      - libssl-dev
      - libx11-dev
      - qtbase5-dev
      - qt5-style-plugins
      - libxinerama-dev
      - libxrandr-dev
      - libxrender-dev
      - libxtst-dev
      - qtdeclarative5-dev
      - libavahi-common-dev
      - libqt5svg5-dev
    stage-packages:
      - libxinerama1
      - libxrandr2
      - libxrender1
      - libqt5gui5
      - libqt5x11extras5
      - libqt5svg5
      - libxtst6
      - libavahi-client3
      - libavahi-common3
      - libavahi-compat-libdnssd1
    after: [desktop-qt5]

  fix-icon:
    plugin: nil
    override-build:
      sed -i 's|Icon=io.github.bryceishunter.leapdesk-kvm|Icon=/usr/share/icons/hicolor/scalable/apps/io.github.bryceishunter.leapdesk-kvm.svg|' $SNAPCRAFT_STAGE/usr/share/applications/io.github.bryceishunter.leapdesk-kvm.desktop
    after: [leapdesk-kvm]
