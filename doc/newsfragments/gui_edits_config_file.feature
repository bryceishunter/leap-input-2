The settings window edits the server's configuration file itself instead of a
copy kept in its own settings. It shows the screens laid out by their links,
lets the part of an edge a link covers be dragged, and edits exact percentages
in a table; everything it doesn't edit, including hotkeys it can't show, is kept
as it was in the file.
